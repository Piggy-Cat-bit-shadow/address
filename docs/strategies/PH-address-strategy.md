# PH 菲律宾地址生成策略

| 项目 | 核心内容 |
|---|---|
| 主源 | Geofabrik `philippines`；PHLPost Zip Code Locator |
| 格式 / 邮编 | `<house> <street>, <barangay>, <city/municipality>, <province> <postcode>, PH`；格式门禁 4 位数字。源记录缺邮编时，仅允许按 PHLPost 的省份 + 城市/市镇唯一匹配补全；同名或冲突记录淘汰，不按邻近地址补齐。 |
| 行政区 | 保留 OSM `addr:barangay` 并映射为最低行政层；PSA PSGC 只做省、市镇、Barangay 名称核验，不生成住宅门牌 |
| 住宅证据 | OSM 住宅建筑标签；barangay 名称不能证明住宅。住宅证据必须来自明确建筑/用途字段，不能由地址存在推断 |
| 发布门禁 | 质量高于数量。仅发布 E3：地址存在与独立住宅用途证据同时成立；本国格式规定的必填组件缺一即淘汰，字段冲突拒绝合并；只允许可逆、可验证的格式规范化。 |
| 同步频率 | 每日以 HEAD 检查 OSM PBF，并从 PHLPost 官方 sitemap 读取 Zip Code Locator 的 `lastmod`；元数据不变时不下载邮编正文。变化后才执行完整同步，PHLPost 正文使用稳定语义指纹并忽略 nonce、统计脚本和 HTML 属性。每个来源版本只构建一次；有增长后等待下一次探测，无增长则标记来源耗尽。 |
| 验证 / 排除 | PHLPost 只核验四位邮编；城市或省级字段缺失时不按邮编反推行政区。PSA PSGC 只提供行政目录，不用于补造门牌。 |
| 策略版本 / 状态 / 更新 | 1.8 / 邮编元数据轻量探测 + 单版本单次构建 + 有限失败暂停 / 2026-08-07 |

- 默认 active 地址上限 10,000；大区、省、市县、Barangay 单节点上限 2,500/500/150/40，后台可覆盖；只裁剪地址记录，行政区划与邮编目录保持完整。

- OSM 独立地址点仅在精确落入明确住宅建筑面时获得住宅用途证据；不使用附近建筑、同街道或邻近坐标推断。
- 临时失败最多连续重试 8 次，随后暂停到下一探测窗口；单任务和外部导出进程均有硬超时，禁止长期占用队列。


默认同步生命周期为自动初始化一次；未完成的额度或 checkpoint 任务自动续跑，完整扫描或来源耗尽后停止，不按日/月重复执行。只有来源、上游版本、严格提取能力变化或管理员明确刷新才重新运行；周期元数据探测仅在显式启用时执行。

统一证据等级、许可、配额与 VPS 边界见 [数据源与自动同步方案](../data-sources.md)。策略变化时同步更新本文件、实现与测试。
## 覆盖与保留

- 官方行政区目录定义覆盖分母；只有关联到官方节点的严格住宅地址计入分子。
- 每个有合格数据的最低行政区先满足每节点、省市和单节点目标，再轮询分配额外记录；国家总量是完成下限，节点规则可推动总量继续增长，来源或分片容量仍是技术硬限制。
- 每次国家快照发布后自动重建住宅覆盖；生成器只展示至少有 1 条可发布住宅地址的官方区域。


## 运行时随机生成

- 公开普通生成只从当前筛选范围内通过发布门禁的完整数据库候选集选择，不使用固定候选窗口、固定顺序或国家特例。
- 服务启动后由最多 4 个按国家分区的只读 worker 建立地址引用与筛选索引；每次按 seed 从完整候选集选择引用，再按主键从 PostgreSQL 读取完整地址及证据。
- 未传入 seed 时由服务器为每个请求生成新 UUID；相同显式 seed 在同一数据库快照中可复现。数据库提交后，新 worker 快照全部就绪才原子替换旧快照。


## Google 住宅地址补全

- 数据源：Geofabrik OSM 明确标记为住宅的建筑面作为住宅证据；Google Geocoding 只反向补全地址字段，不单独证明住宅。
- 调度：优先查询官方目录中零覆盖、未达最低数量和一级/二级数量不足的节点。目录中心点仅用于种子排序；最终行政归属仍由 Google 响应通过官方目录与坐标门禁后确定。所有目录城市统一计入最低行政层分母，不因分支深度不同而遗漏。
- 真实字段：Google Geocoding v4 返回的门牌、道路、行政区、邮编、WGS84 坐标和 Place ID；同一次反向响应中同国家的邮编和行政结果可补全街道结果缺失的层级。OSM 返回建筑 ID、住宅建筑分类和 polygon。生成字段：无。
- 同步：官方批量来源优先；仅在部署者已确认授权并配置 `google-geocoding` 凭据时执行。先探测 50 个分散住宅种子，至少 1 条通过全部严格门禁才扩大到每轮最多 1,000 次；门禁失败按匿名原因计数，不保存原始响应。checkpoint 按适配器版本、上游 PBF 版本和提取策略隔离，缓存清理保留未完成状态并在完成后自动删除。达到 pilot 门槛后连续 3 个有实际请求的批次没有最终发布或三重目标进展时，当前来源指纹自动标记已达上限。缺 Key、额度冷却或短时限流不进入来源耗尽状态。
- 配额：官方免费用量为每个结算账户每月 10,000 次；自动同步默认使用 9,000 次月度预算和 5 QPS。同一项目的 Key 共用额度范围，接入前本月用量由管理员录入基线，达到预算后自动等待下月。
- 门禁：只接受正确国家、完整必填层级、`street_address/premise/subpremise`、高精度位置且坐标落在住宅 polygon 内或距内部点不超过 15 米的结果；核心字段使用本国语言。
- 去重：以 OSM 建筑 ID + Google Place ID 作为来源记录 ID，再执行统一 canonical hash 去重。完整扫描无总量、覆盖或节点进展后，当前来源能力指纹持久标记已达上限。
- 适配器版本：`osm-explicit-residential-google-geocoding-v8`；2026-08-19。启用前必须确认长期保存与再分发授权；低于探测门禁的来源版本自动停止且不重复查询。
